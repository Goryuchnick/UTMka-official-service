# UTMka — конструктор UTM‑меток для маркетологов и бизнеса

**UTMka** — мощный и простой инструмент для маркетологов и владельцев бизнеса, позволяющий:
- **быстро создавать безошибочные UTM‑метки**;
- **сохранять их в шаблоны**;
- **вести историю кампаний и проектов**;
- **экспортировать и импортировать** данные.

Интерфейс выполнен в современном стиле (Tailwind, светлая/тёмная тема), но при этом работает как нативное десктоп‑приложение.

---

## 📥 Скачивание и установка

### Windows

#### Способ 1: Установка через установщик (рекомендуется)

1. **Запустите установщик**:
   - Откройте файл `UTMka_Setup.exe`
   - Следуйте инструкциям мастера установки
   - По умолчанию приложение установится в `C:\Users\<Ваше_имя>\AppData\Local\Programs\UTMka`

2. **Запустите приложение**:
   - Откройте UTMka из меню Пуск или с рабочего стола (если создавали ярлык)

#### Способ 2: Портативная версия (без установки)

1. **Распакуйте и запустите**:
   - Распакуйте архив в любую папку (например, `C:\Programs\UTMka`)
   - Запустите `UTMka.exe` напрямую
   - Все данные будут храниться рядом с исполняемым файлом

> **Примечание**: Для портативной версии убедитесь, что у вас установлен [Microsoft Edge WebView2 Runtime](https://developer.microsoft.com/en-us/microsoft-edge/webview2/), если приложение его запросит.

### macOS

#### Выбор версии

Выберите версию в зависимости от вашего Mac:

- **UTMka-2.0.0-macOS-ARM.dmg** — для Mac с процессором Apple Silicon (M1, M2, M3 и новее)
- **UTMka-2.0.0-macOS-Intel.dmg** — для Mac с процессором Intel

Чтобы узнать, какой процессор у вашего Mac:
1. Нажмите на логотип Apple в левом верхнем углу
2. Выберите "Об этом Mac"
3. Посмотрите на строку "Процессор" или "Чип"

#### Установка

1. **Скачайте DMG файл** для вашей архитектуры
2. **Откройте DMG файл** — он появится в Finder
3. **Перетащите UTMka.app** в папку Applications
   - Или дважды кликните на приложение для запуска без установки

#### Первый запуск

При первом запуске macOS может показать предупреждение о безопасности:

1. Откройте **Системные настройки** → **Безопасность и конфиденциальность**
2. Нажмите **"Открыть в любом случае"** рядом с сообщением о UTMka

**Альтернативный способ** (через Terminal):
```bash
xattr -cr /Applications/UTMka.app
```

> **Подробные инструкции**: См. файл `INSTALL_MACOS.md` для полной инструкции по установке и решению проблем.

---

## ⚡ Производительность и загрузка

- Интерфейс отображается **сразу** после запуска.
- История и шаблоны подгружаются **в фоне** и не должны “подвешивать” окно на старте.

---

## ⚡ Быстрый запуск из исходников

Если вы хотите запустить приложение из исходного кода для разработки или тестирования:

```bash
# Клонируйте репозиторий
git clone https://github.com/Goryuchnick/UTMka-official-service.git
cd UTMka-official-service

# Установите зависимости
pip install -r requirements.txt

# Запустите приложение
python app.py
```

---

## 🎯 Основной функционал

- **Конструктор UTM‑ссылок**
  - базовый URL + `utm_source`, `utm_medium`, `utm_campaign`, `utm_content`, `utm_term`;
  - подсказки по полям, сохранение и повторное использование значений;
  - быстрый копипаст и просмотр полной ссылки.

- **Шаблоны**
  - сохранение часто используемых наборов UTM‑параметров;
  - теги и цветовые метки для группировки по проектам и клиентам;
  - быстрый фильтр и поиск шаблонов.

- **История**
  - автоматическое сохранение всех сгенерированных ссылок;
  - удобный список с датами и действиями (копировать, открыть, удалить);
  - сортировка и фильтрация.

- **Экспорт / импорт**
  - экспорт/импорт **шаблонов** и **истории** в JSON и CSV;
  - встроенные примеры файлов для разных рынков:
    - `templates_example.json`, `templates_example.csv` — базовый набор;
    - `templates_example_ru.json` — примеры для русскоязычного рынка;
    - `templates_example_en.json` — примеры для англоязычного рынка.

---

## 🔒 Локальная офлайн‑версия и её преимущества

UTMka из этого репозитория — это **полностью локальное офлайн‑приложение**:

- **Все данные хранятся только у вас**
  - база данных `utm_data.db` — обычный файл SQLite рядом с приложением (или в папке установки);
  - история, шаблоны, теги — не отправляются на внешние серверы и не анализируются третьими лицами.

- **Никаких логинов и паролей**
  - нет регистрации и аккаунтов;
  - приложение готово к работе сразу после установки.

- **Никаких подписок и оплат**
  - приложение полностью бесплатно;
  - нет ограничений по количеству ссылок, тарифов, API‑ключей и т.п.

- **Работает без интернета**
  - генерация UTM‑ссылок, история и шаблоны работают полностью офлайн;
  - интернет нужен только для внешних шрифтов/иконок (и то опционально).

В будущем может появиться **облачная веб‑версия** с аккаунтами и общей базой данных,  
но локальная редакция останется вариантом **«всё храню у себя и ни от кого не завишу»**.

---

## 📂 Структура проекта

- `app.py` — классическая десктоп‑версия (Flask + PyWebView) для Windows
- `app_qtwebengine.py` — десктоп‑версия на PyQt6 + QtWebEngine (используется для macOS)
- `native_app/` — нативная PyQt‑версия без webview
  - `native_app/core/db.py` — работа с локальной БД SQLite
  - `native_app/core/services.py` — логика UTM/шаблонов/истории
  - `native_app/ui/main_window.py` — главное окно PyQt6
  - `native_app/main.py` — точка входа нативного UI
- `index.html` — HTML‑интерфейс приложения (используется web‑вариантами)
- `requirements.txt` — список зависимостей
- `logo/` — логотипы и иконки приложения
- `*.iss` — скрипты Inno Setup для Windows установщиков
- `UTMka_macos_arm.spec` — спецификация PyInstaller для macOS ARM (Apple Silicon)
- `UTMka_macos_intel.spec` — спецификация PyInstaller для macOS Intel (x86_64)
- `scripts/` — скрипты сборки
  - `build_macos_arm.sh` — сборка для Apple Silicon
  - `build_macos_intel.sh` — сборка для Intel
  - `create_dmg_arm.sh` — создание DMG установщика для ARM
  - `create_dmg_intel.sh` — создание DMG установщика для Intel
  - `build.py` — скрипт сборки для Windows

---

## 🛠 Технологии

- **Python / Flask** — веб‑сервер и API для десктоп‑версий
- **PyWebView** — нативное окно поверх встроенного браузерного движка (классическая версия для Windows)
- **PyQt6 + QtWebEngine** — нативное приложение с браузерным движком Qt (версия для macOS)
- **PyQt6 (Widgets)** — полностью нативный UI без web‑слоя (`native_app`)
- **SQLite** — локальная файловая база данных
- **Tailwind CSS, Lucide Icons, Flatpickr** — фронтенд‑стек HTML‑интерфейса (`index.html`)
- **PyInstaller** — упаковка приложения в исполняемые файлы (.exe для Windows, .app для macOS)

---

## 👤 Разработчик

UTMka разработана **Александром Прониным**.

- **Сайт**: [alex-pronin.ru](https://alex-pronin.ru)
- **Telegram‑канал**: [t.me/pronin_marketing](https://t.me/pronin_marketing)

В подвале приложения есть быстрые ссылки на сайт и Telegram с полезными материалами по маркетингу и аналитике.  
Если у вас есть идеи по улучшению UTMka, баг‑репорты или пожелания — лучше всего писать туда **или создавать задачи на GitHub** (Issues / Discussions) в репозитории проекта.

---

## 🐛 Предложения и баг‑репорты

Используйте раздел **Issues** на GitHub, чтобы:
- сообщить об ошибке;
- предложить улучшение или новый функционал;
- обсудить идеи по развитию приложения.

Чем подробнее вы опишете проблему (шаги воспроизведения, скриншоты, версия Windows/приложения), тем проще будет её исправить.

---

## 📄 Лицензия

Этот проект распространяется свободно. Используйте его как вам угодно.

---

**Приятной работы с UTMka!** 🚀
