# UTMka 3.0 — промпт для продолжения работы

> Скопировать целиком в новый чат Claude Code, запущенный из корня workspace
> (`d:/Programmes projects`). Промпт самодостаточный — состояние восстанавливается из репозитория.
>
> Обновлён 2026-08-12. **Инструмент в проде**, лендинг на сайте выкачен, ветка запушена.

---

```
Продолжаем UTMka 3.0 — веб-версию конструктора UTM-меток. Работа идёт в ветке v3.0
репозитория products/UTMka-official-service/. В main лежит выпущенный десктоп 2.2.1 —
туда не коммитим до слияния.

ПЕРЕД НАЧАЛОМ ПРОЧИТАЙ (источники правды, решения не переспоривать):
- products/UTMka-official-service/CLAUDE.md — раскладка репозитория и правила ядра
- products/UTMka-official-service/docs/ARCHITECTURE.md — архитектура целиком,
  журнал решений в §11
- products/UTMka-official-service/.claude/docs/SECURITY-AUDIT.md — аудит перед продом,
  закрытые дыры и принятые остаточные риски
- products/UTMka-official-service/docs/ASSISTANT-SPEC.md — три слоя помощника
- products/UTMka-official-service/docs/DESIGN-BRIEF.md — визуал и три оболочки

## Что уже сделано (не переделывать)

ШАГИ 0–6 закрыты. Инструмент живёт на https://utmka.alex-pronin.ru

packages/core (@utmka/core) — слой правил, 141 тест, чистый TS без зависимостей:
build (паритет с 2.2), parse, validate, normalize, presets (8 площадок),
dictionary (автодополнение, детектор расщепления), batch (CSV), redirect
(цепочка + предохранители SSRF), hints, repository (порты хранилища).

apps/web — Next.js 16.3 + React 19 + Tailwind v4:
- каркас «ПРОНИН-ОС»: рамка устройства, CRT, стеклянная панель разделов,
  планка помощника, строка состояния, мобильный док;
- генератор в двух режимах (простой и расширенный), общая модель данных,
  заготовка из адресной строки (?url=…&source=…);
- свой календарь-поповер на Motion, привязанный к кнопке поля;
- QR-код и короткая ссылка через /api/shorten (clck.ru, с сервера);
- разбор чужой ссылки (/parse) и пакетный режим (/batch);
- кодовая фраза (/login), история (/history), шаблоны и справочник значений
  (/templates), помощь (/help);
- помощник: бриф → пакет меток через routerai, ответ модели всегда прогоняется
  через normalize+validate ядра, квота дневная и видимая (50/сутки на фразу);
  результат можно отдать в пакетный режим кнопкой — таблица заполнится сама;
- маскот — спрайты сайта (пиксельный Александр), реагирует настроением:
  говорит на новой реплике, кивает на готовое, удивляется замечаниям,
  думает, пока считает модель;
- обучение — тур с подсветкой реальных элементов (вырез clip-path вокруг цели
  + рамка + карточка рядом, механика 2.2), шаги учитывают пакет, разбор,
  помощника и фразу; при первом визите и по кнопке в помощи;
- помощь в две колонки: слева объяснения, справа обучение, версия для ПК
  (прямые ссылки на релизы), исходники, issues, Telegram, донаты;
- QR выгружается в PNG и SVG, история и шаблоны — импорт и выгрузка JSON/CSV;
- SEO: OG-плашка на лету, robots, sitemap, JSON-LD WebApplication,
  верификация в обеих панелях;
- ограничение частоты на всех уязвимых роутах, заголовки безопасности,
  потолок тела запроса.

Пиксельный шрифт — Press Start 2P через next/font (у Karmatic Arcade кириллица
в cmap есть, а глифы пустые — не возвращать).

Лендинг на сайте: personal/my-site/my-own-site-pronin-alex/, маршрут /tools/utmka.
Конструктор оттуда УДАЛЁН (UtmkaApp.tsx), осталась содержательная страница про
UTM + блок офлайн-версии.

Деплой: бокс sites-mine (5.129.197.27), сервис `utmka` в /opt/stack,
env /opt/stack/env/utmka.env (600), vhost в /opt/stack/Caddyfile.
Заливка — tar-over-ssh, ключ ~/.ssh/id_ed25519_nalogovka_deploy.

## ⚠️ Что осталось до «веб закрыт полностью»

Владелец 2026-08-12: на Rust идём ТОЛЬКО после того, как веб дотянут до
паритета — иначе десктоп унаследует дыры в дизайне.

Незакрытый паритет с 2.2 (список функций — ARCHITECTURE §4.1):
1. **RU/EN.** Интерфейс только русский, в 2.2 был словарь и тумблер языка.
   Готовая основа — `legacy/desktop-2.2/frontend/js/translations.js`. Работа
   не сложная, но широкая: строк под сотню, трогает каждый экран. Делать
   отдельным заходом, а не «между делом».
2. **Модалка деталей ссылки:** детали + «сократить» / QR / «применить».

Сделано 2026-08-12: фильтр истории по датам, быстрый старт (недавние шаблоны
в генераторе), QR в SVG, импорт истории, донаты в шапке и строке состояния.

Отдельно, не паритет:
5. **Ротировать ключ `sb_secret_…`**, показанный в переписке 2026-08-11.
   Приложение на него не завязано (работает под service-role JWT).

Сделано 2026-08-12: схема `utmka` в Exposed schemas, хранилище проверено
end-to-end (регистрация фразы, сессия, история, справочник), тестовые записи
вычищены. Домен подтверждён в Яндекс.Вебмастере и Search Console, sitemap
отправлен в обе панели. Ветка `v3.0` запушена в GitHub.

## Что дальше — по желанию владельца

- ШАГ 7: десктоп на Rust/Tauri поверх того же packages/core.
- Проверка редиректов (/api/redirect-check): логика в ядре готова и покрыта
  тестами, роута ещё нет.
- Импорт/экспорт истории в шаблоны, быстрый старт из недавних шаблонов.
- Отдельная роль `utmka_service` в Supabase вместо общего service-role
  (остаточный риск из аудита §7).

## Деньги на помощника (замер 2026-08-12)

Цены routerai для `qwen3-30b-a3b-instruct`: **5,16 ₽/млн токенов входа**,
**20,67 ₽/млн выхода** (из `GET /models`, поле `pricing`). Типовой запрос —
0,64 коп., максимальный (бриф 1200 символов, 12 ссылок) — 1,21 коп.

Человек, выжигающий квоту 50/сутки каждый день, стоит ~18 ₽/мес. Сто таких —
1800 ₽/мес, но это сценарий, где каждый жжёт всё до дна. Keep-alive раз в
3 минуты — 3 ₽/мес. Вывод: платная модель не нужна, лимит можно поднимать
дальше по мере роста.

## Стек — зафиксирован, не пересматривать

TypeScript/Next.js для веба целиком, включая серверную часть. Go в этом проекте
не используется — см. .claude/docs/STACK.md §2.1. Десктоп — Rust/Tauri.

## Правила работы

- Дизайн-система наследуется от «ПРОНИН-ОС»: токены --hv2-*, стекло .pexe-glass,
  иконки PixelIcon. Своих цветов и шрифтов не заводить.
- Персональных данных не собираем нигде. Кодовая фраза именно для этого.
- Инструмент обязан работать до входа. Всё, что хранится, — за фразой.
- Никаких платных тарифов и апселлов.
- Перед коммитом: npm test и npm run type-check в корне, затем /review-code.
- Перед деплоем: /secure-audit.
- Отвечай по-русски.
```
