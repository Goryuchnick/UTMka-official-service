# UTMka 3.0 — промпт для продолжения работы

> Скопировать целиком в новый чат Claude Code, запущенный из корня workspace
> (`d:/Programmes projects`). Промпт самодостаточный — состояние восстанавливается из репозитория.
>
> Обновлён 2026-08-11, после ШАГА 3 (первая половина).

---

```
Продолжаем UTMka 3.0 — веб-версию конструктора UTM-меток. Работа идёт в ветке v3.0
репозитория products/UTMka-official-service/. В main лежит выпущенный десктоп 2.2.1 —
туда не коммитим до слияния.

ПЕРЕД НАЧАЛОМ ПРОЧИТАЙ (источники правды, решения не переспоривать):
- products/UTMka-official-service/CLAUDE.md — раскладка репозитория и правила ядра
- products/UTMka-official-service/docs/ARCHITECTURE.md — архитектура целиком,
  журнал решений в §11
- products/UTMka-official-service/docs/ASSISTANT-SPEC.md — три слоя помощника
- products/UTMka-official-service/docs/DESIGN-BRIEF.md — визуал и три оболочки

## Что уже сделано (не переделывать)

ШАГ 0 — архитектура. ШАГ 1 — ядро. ШАГ 2 — дизайн утверждён. ШАГ 3 — наполовину.

packages/core (@utmka/core) — слой правил, 141 тест, чистый TS без зависимостей:
build (паритет с 2.2), parse, validate (проверки с объяснением последствий),
normalize, presets (8 площадок), dictionary (автодополнение, детектор расщепления),
batch (CSV), redirect (цепочка + предохранители SSRF), hints (типовые значения),
repository (порты хранилища).

apps/web — Next.js 16 + React 19 + Tailwind v4:
- каркас «ПРОНИН-ОС»: рамка устройства, экран с CRT, стеклянная панель разделов
  на верхней кромке, планка помощника под ней (фиксированная высота), строка
  состояния на нижней, мобильный док со сворачиванием в кнопку;
- генератор в двух режимах: простой (4 шага, плитки пресетов) и расширенный
  (5 полей, панель замечаний), общая модель данных;
- поля с подсказками значений и кнопкой даты (дата дописывается через `_`);
- QR-код и короткая ссылка через /api/shorten (clck.ru, с сервера);
- разбор чужой ссылки (/parse) и пакетный режим (/batch);
- тема светлая/тёмная, пиксельный шрифт Karmatic Arcade в заголовках.

Заглушки (компонент Soon) на /history, /templates, /help, /login — их и доделываем.

## Что осталось — по порядку

ШАГ 3, вторая половина: кодовая фраза и хранение.
1. Порт passphrase-логина из personal/my-site/my-own-site-pronin-alex/
   (lib/passphrase.ts, lib/auth/session.ts). Секрет СВОЙ: UTMKA_PASSPHRASE_HMAC_SECRET.
2. Supabase — существующий проект vmrqocznhydwmkkaochf, отдельная схема `utmka`
   (схему добавить в PostgREST → Exposed schemas). Миграции лежат в этом репозитории:
   apps/web/supabase/migrations/utmka_0001_init.sql. Схема таблиц — ARCHITECTURE §5.2.
3. Экраны /login, /history, /templates: шаблоны с тегами и цветами, справочник
   значений со сведением расщеплений, история с поиском и тремя видами отображения
   (список/плитки/таблица), импорт-экспорт JSON и CSV. Всё это было в 2.2 — паритет
   обязателен, список в ARCHITECTURE §4.1.
4. Экран /help.
   → остановка

ШАГ 4. Помощник: окно с маскотом. Сначала комментарии на правилах (уже частично есть
в планке), потом LLM-сценарий «бриф → пакет меток» через routerai с видимым лимитом
на кодовую фразу. Ответ модели ВСЕГДА прогоняется через ядро перед выдачей.
   → остановка

ШАГ 5. Лендинг на alex-pronin.ru/utmka. ВАЖНО: инструмент уехал на субдомен, поэтому
весь поисковый вес несёт лендинг — это не заглушка с кнопкой, а полноценная страница:
что такое UTM, частые ошибки, примеры для Директа и ВК, FAQ, разметка. Живёт в
репозитории personal/my-site/my-own-site-pronin-alex/, маршрут /tools/utmka.
⚠️ Сам конструктор с этой страницы УДАЛЯЕТСЯ (components/tools/UtmkaApp.tsx),
блок офлайн-версии UtmkaOffline.tsx остаётся.
   → остановка

ШАГ 6. Деплой на VPS sites-mine (5.129.197.27), домен utmka.alex-pronin.ru.
Паттерн /opt/stack: docker-compose + Caddy, tar-over-ssh. Грабли — в ARCHITECTURE §8
и .claude/docs/INFRA.md: не править Caddyfile через sed (bind-mount по иноду),
сверять список *-edge сетей у caddy перед заливкой compose, чистить build cache,
mem_limit 400m + NODE_OPTIONS=--max-old-space-size=256.
Перед деплоем — /secure-audit.

## Стек — зафиксирован, не пересматривать

TypeScript/Next.js для веба целиком, включая серверную часть: логика тонкая
(Supabase + routerai + два внешних запроса), отдельный Go-сервис был бы третьим
языком без выигрыша. Go в этом проекте не используется — см. .claude/docs/STACK.md §2.1.
Десктоп после веба — Rust/Tauri, поверх того же packages/core.

## Каких агентов и скиллов звать

- Фронтенд: агент frontend-dev. Серверная часть, Supabase, routerai: backend-dev.
- Тексты помощника, лендинга, микрокопия: copywriter (голос — .claude/knowledge/me/bio.md,
  без кликбейта). SEO лендинга: seo-specialist.
- Перед коммитом: npm test и npm run type-check в корне, затем /review-code.
- Перед деплоем: /secure-audit, затем агент devops.

## Правила работы

- После каждого шага останавливайся и жди одобрения.
- Дизайн-система наследуется от «ПРОНИН-ОС» (репозиторий сайта): токены --hv2-*,
  стекло .pexe-glass, иконки PixelIcon. Своих цветов и шрифтов не заводить.
- Персональных данных не собираем нигде. Кодовая фраза именно для этого.
- Инструмент обязан работать до входа. Всё, что хранится, — за фразой.
- Никаких платных тарифов и апселлов.
- Отвечай по-русски.

Начни с ШАГА 3, вторая половина: passphrase-логин и Supabase.
```
