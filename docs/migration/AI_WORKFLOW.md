# Гайд по работе с AI агентами в Cursor

## Введение

Этот гайд поможет эффективно использовать AI для работы над проектом UTMka. Описаны рекомендации по выбору моделей, формулировке задач и организации работы.

---

## Доступные модели в Cursor AI

### Топ-уровень (сложные архитектурные задачи)

| Модель | Лучше всего для | Скорость | Качество |
|--------|-----------------|----------|----------|
| **Opus 4.5** | Сложнейшие задачи, архитектура, рефакторинг | Медленная | Превосходное |
| **Composer 1** | Мульти-файловые изменения, агентная работа | Средняя | Превосходное |

### Высокий уровень (основная работа)

| Модель | Лучше всего для | Скорость | Качество |
|--------|-----------------|----------|----------|
| **Sonnet 4.5** | Сложные задачи, баланс качества и скорости | Средняя | Отличное |
| **GPT-5.2 Codex High** | Генерация кода высокого качества | Средняя | Отличное |
| **Gemini 3 Pro** | Сложные задачи, большой контекст | Средняя | Отличное |

### Средний уровень (повседневные задачи)

| Модель | Лучше всего для | Скорость | Качество |
|--------|-----------------|----------|----------|
| **Sonnet 4** | Стандартные задачи, рефакторинг | Быстрая | Хорошее |
| **GPT-5.2 Codex** | Генерация кода, автодополнение | Быстрая | Хорошее |
| **GPT-5.2** | Общие задачи, документация | Быстрая | Хорошее |
| **Grok Code** | Кодирование, отладка | Быстрая | Хорошее |

### Быстрые модели (простые задачи)

| Модель | Лучше всего для | Скорость | Качество |
|--------|-----------------|----------|----------|
| **Gemini 3 Flash** | Много мелких файлов, быстрые правки | Очень быстрая | Хорошее |
| **GPT-5.1 Codex Mini** | Простые правки, автодополнение | Очень быстрая | Базовое |
| **Haiku 4.5** | Мелкие задачи, быстрые ответы | Очень быстрая | Базовое |

---

## Стратегия выбора моделей для не-MAX тарифа

### Принципы экономии запросов:

1. **Дорогие модели (Opus 4.5, Composer 1)** — только для критически важных задач
2. **Средние модели (Sonnet 4.5, GPT-5.2 Codex High)** — основная работа
3. **Быстрые модели (Gemini 3 Flash, Sonnet 4)** — для рутинных задач и итераций

### Когда использовать дорогие модели:
- Первый раз создаёте сложную архитектуру
- Нужно глубокое понимание всей кодовой базы
- Мульти-файловые изменения, затрагивающие много зависимостей

### Когда можно использовать быстрые:
- Повторяющиеся паттерны (создание похожих файлов)
- Мелкие правки и рефакторинг
- Итерации после основной работы

---

## Рекомендации по этапам

### Этап 1: Реструктуризация

**🎯 Оптимальный выбор: Sonnet 4.5**

**Почему именно Sonnet 4.5:**
- Отличный баланс качества и стоимости
- Хорошо понимает архитектуру и зависимости между файлами
- Эффективно работает с несколькими файлами одновременно
- Достаточно умён для рефакторинга без лишних итераций

**💡 Когда использовать Opus 4.5 или Composer 1:**
- Если Sonnet 4.5 даёт неудовлетворительные результаты после 2-3 попыток
- При создании сложной структуры с множеством зависимостей
- Когда нужно переписать большие части кода с нуля

**⚡ Альтернатива для экономии:**
- **Sonnet 4** — если задача простая (создание структуры папок, базовые файлы)
- **Gemini 3 Flash** — для создания множества похожих файлов по шаблону

**Примеры промптов:**

```
Создай файл src/core/config.py с функциями get_data_dir() и get_db_path() 
по примеру из docs/migration/STEP_1_RESTRUCTURE.md
```

```
Перенеси модели из web/app/models/ в src/core/models.py, 
объединив в один файл. Сохрани все методы to_dict().
```

```
Создай src/api/__init__.py с функцией create_app() 
которая использует модели из src/core/
```

---

### Этап 2: Frontend

**🎯 Оптимальный выбор: Gemini 3 Flash (основная работа) + Sonnet 4.5 (сложные части)**

**Почему именно Gemini 3 Flash:**
- Очень быстрая и дешёвая
- Отлично справляется с повторяющимися паттернами (создание похожих JS модулей)
- Много мелких файлов — идеально для Flash
- Хорошо понимает современный JavaScript/ES6

**💡 Когда использовать Sonnet 4.5:**
- Создание сложной логики (state management, сложные компоненты)
- Интеграция с API, обработка ошибок
- Когда Flash даёт некачественный код после 1-2 попыток

**⚡ Альтернативы:**
- **Sonnet 4** — хороший компромисс, если Flash не справляется, а Sonnet 4.5 дорого
- **GPT-5.2 Codex** — если нужна генерация большого объёма кода быстро

**📊 Стратегия работы:**
1. Начни с Gemini 3 Flash для создания базовых модулей
2. Если результат не устраивает — переключись на Sonnet 4
3. Sonnet 4.5 используй только для сложных компонентов (генератор, история)

**Примеры промптов:**

```
Извлеки из index.html весь JavaScript код функции generateUTM 
и создай модуль frontend/js/components/generator.js
```

```
Создай frontend/js/api.js с функциями для работы с API:
- getHistory(email)
- addHistory(email, url)
- getTemplates(email)
- addTemplate(template)
Используй fetch и async/await.
```

```
Извлеки CSS стили из index.html в три файла:
- frontend/css/main.css (базовые стили, reset)
- frontend/css/components.css (карточки, кнопки, формы)
- frontend/css/utilities.css (flex, margin, padding утилиты)
```

---

### Этап 3: Windows Installer

**🎯 Оптимальный выбор: GPT-5.2 Codex или Sonnet 4.5**

**Почему именно GPT-5.2 Codex:**
- Специализируется на генерации кода и конфигурационных файлов
- Отлично знает PyInstaller spec файлы и Inno Setup скрипты
- Быстрее и дешевле чем Sonnet 4.5 для таких задач
- Хорошо генерирует шаблонный код (spec файлы, скрипты установки)

**💡 Когда использовать Sonnet 4.5:**
- Если GPT-5.2 Codex не знает специфичных деталей вашего проекта
- Нужна адаптация под конкретную структуру проекта
- Требуется понимание зависимостей между файлами

**⚡ Альтернатива:**
- **Sonnet 4** — если задача простая (базовый spec файл)
- **Opus 4.5** — только если предыдущие модели не справляются (маловероятно)

**📊 Стратегия работы:**
1. Начни с GPT-5.2 Codex — он отлично знает PyInstaller и Inno Setup
2. Если нужна адаптация под проект — переключись на Sonnet 4.5
3. Opus 4.5 используй только в крайнем случае

**Примеры промптов:**

```
Создай PyInstaller spec файл для UTMka с:
- Точка входа: src/desktop/main.py
- Включить папки: frontend/, assets/
- Исключить: tkinter, matplotlib, numpy
- Иконка: assets/logo/logoutm.ico
```

```
Создай Inno Setup скрипт который:
1. Устанавливает в Program Files
2. Создаёт папку данных в AppData
3. Создаёт ярлыки на рабочем столе
4. При удалении спрашивает удалять ли данные
```

---

### Этап 4: macOS

**🎯 Оптимальный выбор: GPT-5.2 Codex или Sonnet 4.5**

**Почему именно GPT-5.2 Codex:**
- Хорошо знает macOS специфику (bundle структура, .icns, DMG)
- Эффективно генерирует shell скрипты для создания DMG
- Быстрее для шаблонных задач (адаптация spec файла под macOS)

**💡 Когда использовать Sonnet 4.5:**
- Нужна адаптация существующего Windows spec файла
- Требуется понимание различий между платформами
- Сложная логика в скриптах сборки

**⚡ Альтернатива:**
- **Sonnet 4** — для простых задач (базовая адаптация)
- **Opus 4.5** — только если нужна сложная кастомизация DMG

**📊 Стратегия работы:**
1. Начни с GPT-5.2 Codex для создания базового macOS spec
2. Sonnet 4.5 используй для адаптации и доработки
3. Opus 4.5 — только для сложных кастомизаций

**Примеры промптов:**

```
Адаптируй PyInstaller spec для macOS:
- BUNDLE для создания .app
- bundle_identifier: ru.utmka.desktop
- Поддержка Dark Mode
- Иконка .icns
```

```
Создай скрипт create_dmg.sh который:
- Создаёт DMG с приложением
- Добавляет ссылку на Applications
- Красивый фон (опционально)
```

---

## Режимы работы в Cursor

### Agent Mode (рекомендуется для большинства задач)

Включён по умолчанию. AI может:
- Читать файлы
- Создавать/редактировать файлы
- Запускать команды
- Работать с несколькими файлами

**Когда использовать:** Для всех задач кроме планирования.

### Plan Mode

AI только планирует, не вносит изменения.

**Когда использовать:**
- Обсуждение архитектуры
- Уточнение требований
- Составление плана работ

**Как включить:** Напишите "plan mode" или используйте команду `/plan`

### Ask Mode

AI только отвечает на вопросы, не меняет файлы.

**Когда использовать:**
- Вопросы по коду
- Объяснение концепций
- Поиск информации

---

## Эффективные промпты

### Структура хорошего промпта:

```
[Контекст] + [Задача] + [Ограничения/требования]
```

### Примеры:

**Плохо:**
```
Сделай API
```

**Хорошо:**
```
Создай Flask API в src/api/routes/history.py для работы с историей UTM-ссылок.
Эндпоинты:
- GET /history?user_email=... - получить историю
- POST /history - добавить запись (body: {user_email, url})
- DELETE /history/<id> - удалить запись

Используй модель HistoryLegacy из src/core/models.py.
Добавь валидацию через Marshmallow.
```

**Плохо:**
```
Исправь баг
```

**Хорошо:**
```
В файле frontend/js/components/generator.js функция handleSubmit 
не обрабатывает случай когда URL уже содержит параметры (?param=value).
Добавь проверку и корректное объединение параметров.
```

---

## Работа с контекстом (@mentions)

Cursor позволяет указывать контекст через @:

```
@src/core/models.py Добавь метод get_by_email в класс User
```

```
@docs/migration/STEP_2_FRONTEND.md Создай файлы по этой инструкции
```

```
@frontend/ Найди все места где используется fetch и замени на функции из api.js
```

---

## Типичные ошибки и решения

### Ошибка: AI создаёт слишком много кода сразу

**Решение:** Разбивайте задачи на мелкие части.

```
# Вместо:
"Создай весь frontend"

# Лучше:
"Создай frontend/js/api.js с функциями для работы с API"
"Создай frontend/js/state.js для управления состоянием"
"Создай frontend/js/components/generator.js"
```

### Ошибка: AI не понимает структуру проекта

**Решение:** Явно укажите пути и зависимости.

```
Создай src/api/routes/utm.py.
Импортируй модели из src.core.models (не из web.app.models).
Используй db из src.core.models.
```

### Ошибка: AI предлагает устаревшие решения

**Решение:** Укажите версии и требования.

```
Используй ES6 модули (import/export), не CommonJS (require).
Используй async/await, не callbacks.
Используй Flask 3.x синтаксис.
```

---

## Чек-листы для каждого этапа

### Перед началом работы:

- [ ] Прочитал инструкцию по этапу
- [ ] Выбрал подходящую модель
- [ ] Разбил задачу на подзадачи

### После завершения подзадачи:

- [ ] Проверил что код работает
- [ ] Нет ошибок линтера
- [ ] Импорты корректны
- [ ] Можно перейти к следующей подзадаче

### После завершения этапа:

- [ ] Все пункты чек-листа из инструкции выполнены
- [ ] Приложение запускается
- [ ] Основные функции работают

---

## Экономия запросов для не-MAX тарифа

### ✅ Используй дешёвые модели для:

1. **Создание похожих файлов** → Gemini 3 Flash
   - Много JS модулей с похожей структурой
   - CSS файлы с повторяющимися паттернами
   - Простые конфигурационные файлы

2. **Мелкие правки и рефакторинг** → Sonnet 4 или Gemini 3 Flash
   - Исправление импортов
   - Переименование переменных
   - Форматирование кода

3. **Итерации после основной работы** → Gemini 3 Flash
   - Добавление комментариев
   - Мелкие улучшения
   - Исправление опечаток

### 💎 Используй дорогие модели только для:

1. **Первое создание сложной архитектуры** → Sonnet 4.5
   - Когда нужно понимание всей структуры проекта
   - Создание ключевых модулей (core, api)

2. **Сложный рефакторинг с зависимостями** → Sonnet 4.5
   - Когда изменения затрагивают много файлов
   - Нужно понимание всех связей

3. **Когда дешёвые модели не справляются** → Sonnet 4.5 или Opus 4.5
   - После 2-3 неудачных попыток с дешёвой моделью
   - Когда результат явно некачественный

### 📊 Пример экономии:

**Плохой подход (дорого):**
- Opus 4.5 для создания структуры папок → ~10 запросов
- Opus 4.5 для каждого JS модуля → ~50 запросов
- **Итого: ~60 запросов к дорогой модели**

**Хороший подход (экономно):**
- Sonnet 4.5 для структуры → ~5 запросов
- Gemini 3 Flash для JS модулей → ~30 запросов (дешёвые)
- Sonnet 4.5 для сложных компонентов → ~5 запросов
- **Итого: ~10 запросов к дорогой модели, ~30 к дешёвой**

**Экономия: ~50 запросов к дорогой модели!**

---

## Советы по продуктивности

### 1. Используйте Chat History

Cursor сохраняет историю чатов. Можно вернуться к предыдущему контексту. Это экономит запросы — не нужно заново объяснять структуру проекта.

### 2. Делайте коммиты после каждого этапа

```bash
git add .
git commit -m "Этап 1: Реструктуризация папок"
```

Это позволит откатиться если что-то пойдёт не так.

### 3. Тестируйте после каждого изменения

Не накапливайте изменения. Проверяйте что работает после каждой подзадачи.

### 4. Используйте Composer для сложных задач

Composer (Cmd+I / Ctrl+I) позволяет работать с несколькими файлами одновременно. Модель Composer 1 оптимизирована для мульти-файловых операций.

### 5. Сохраняйте удачные промпты

Если промпт дал хороший результат, сохраните его для будущего использования.

---

## Пример рабочей сессии (с учётом экономии)

```
# Сессия 1: Создание структуры (экономный подход)
1. Открыть Cursor
2. Выбрать Sonnet 4.5 (не Opus 4.5 - экономия!)
3. Промпт: "Создай структуру папок по docs/migration/ARCHITECTURE.md"
4. Проверить созданные папки
5. Если что-то не так - уточнить промпт, не переключаться на Opus
6. Коммит: "chore: создана структура папок"

# Сессия 2: Core модуль (начинаем с дешёвой модели)
1. Выбрать Sonnet 4 (попробовать сначала дешёвую)
2. Промпт: "Создай src/core/config.py по инструкции из STEP_1"
3. Если результат хороший - продолжаем с Sonnet 4
4. Если не устраивает - переключиться на Sonnet 4.5
5. Промпт: "Создай src/core/models.py, перенеси модели из web/app/models/"
6. Проверить импорты
7. Коммит: "feat: добавлен core модуль"

# Сессия 3: API (используем специализированную модель)
1. Выбрать GPT-5.2 Codex (специализация на код)
2. Промпт: "Создай src/api/__init__.py с create_app()"
3. Если нужна адаптация - переключиться на Sonnet 4.5
4. Промпт: "Перенеси роуты из web/app/routes/ в src/api/routes/"
5. Исправить импорты (можно использовать Gemini 3 Flash для мелких правок)
6. Протестировать API
7. Коммит: "feat: перенесён API в src/api"

# Сессия 4: Frontend (максимальная экономия)
1. Выбрать Gemini 3 Flash (дешёвая и быстрая)
2. Промпт: "Создай frontend/js/api.js с функциями для работы с API"
3. Если результат хороший - продолжаем с Flash
4. Для сложных компонентов переключиться на Sonnet 4.5
5. Промпт: "Создай frontend/js/components/generator.js"
6. Мелкие правки делать с Flash
7. Коммит: "feat: добавлен frontend"
```

---

## Резюме для не-MAX тарифа

| Этап | 🎯 Основная модель | 💰 Почему | ⚡ Альтернатива | 🚀 Для экономии |
|------|------------------|-----------|-----------------|-----------------|
| 1. Реструктуризация | **Sonnet 4.5** | Баланс качества/стоимости, понимает архитектуру | Opus 4.5 (если не справляется) | Sonnet 4 (простые задачи) |
| 2. Frontend | **Gemini 3 Flash** | Быстро, дешево, много похожих файлов | Sonnet 4.5 (сложные компоненты) | Sonnet 4 (компромисс) |
| 3. Windows | **GPT-5.2 Codex** | Знает PyInstaller/Inno Setup, специализация | Sonnet 4.5 (адаптация) | Sonnet 4 (базовые задачи) |
| 4. macOS | **GPT-5.2 Codex** | Знает macOS специфику, shell скрипты | Sonnet 4.5 (адаптация) | Sonnet 4 (базовые задачи) |

### 💡 Практические советы:

1. **Начинай с дешёвых моделей** — часто Gemini 3 Flash или Sonnet 4 справятся
2. **Переходи на дорогие только при необходимости** — если результат не устраивает после 2-3 попыток
3. **Используй дорогие модели для планирования** — один раз запланируй с Opus 4.5, потом выполняй с дешёвыми
4. **Разбивай задачи** — мелкие задачи = меньше запросов к дорогим моделям
5. **Сохраняй контекст** — используй @mentions чтобы не тратить запросы на объяснение структуры

**Главное правило:** Начинай с дешёвых моделей, переходи на дорогие только если результат не устраивает.
