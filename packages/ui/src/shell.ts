/**
 * Мост к оболочке.
 *
 * Экраны импортируют отсюда, а сам файл — тонкий реэкспорт из алиаса `#shell`,
 * который каждое приложение направляет в свою реализацию:
 *
 * | приложение   | `#shell`                      |
 * |--------------|-------------------------------|
 * | `apps/web`   | `apps/web/lib/shell.ts`       |
 * | `apps/desktop` | `apps/desktop/src/lib/shell.ts` |
 *
 * Почему алиас, а не React-контекст: рамка устройства рисуется в layout, а окно
 * помощника живёт внутри неё — провайдер пришлось бы тащить через всё дерево.
 * Почему не синглтон с `install()`: он даёт порядковую зависимость «поставить
 * до первого рендера» и падает в рантайме, а алиас проверяется типами на сборке.
 */

export { backend, saveFile, useNav, useNavParams, NavLink } from '#shell'
