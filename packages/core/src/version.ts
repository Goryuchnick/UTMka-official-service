/**
 * Версия продукта — одна строка на обе оболочки.
 *
 * До этого число стояло вписанным прямо в разметку статус-бара и экрана
 * помощи. Пережить релиз такое не может в принципе: приложение обновилось до
 * 3.1.0, а в углу осталось «3.0» — человек видит старую цифру и решает, что
 * обновление не встало. Настоящих версий у десктопа две (`tauri.conf.json` и
 * `Cargo.toml`), и обе — про сборку, а не про интерфейс; сюда их не затянуть:
 * ядро читает файлы только в тестах.
 *
 * Поэтому источник правды здесь, а расхождение ловит тест `version.test.ts`:
 * он сверяет эту строку с обоими манифестами десктопа и падает на сборке
 * релиза раньше, чем что-то уедет пользователю.
 */
export const PRODUCT_VERSION = '3.1.1'
