/// <reference types="vite/client" />

/**
 * Алиас `#shell` резолвится сборщиком (Vite) и компилятором (`tsconfig.paths`),
 * но модуль лежит в этом же приложении — отдельного объявления ему не нужно.
 * Здесь только типы Vite: импорт CSS и `import.meta.env`.
 */
