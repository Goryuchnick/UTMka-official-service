import { Soon } from '@/components/Soon'

export default function ParsePage() {
  return (
    <Soon
      title="Разбор чужой ссылки"
      line="Вставите ссылку — покажу, что в ней не так и почему рассыплется отчёт."
      what="Разбор по параметрам, список проблем и кнопка «починить». Логика в ядре (parse.ts + validate.ts), собираю экран."
    />
  )
}
